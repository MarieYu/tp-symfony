# To get started with security, check out the documentation:
# http://symfony.com/doc/current/book/security.html
security:
    encoders:
        Symfony\Component\Security\Core\User\User: plaintext
        UserBundle\Entity\User: plaintext

    role_hierarchy:
        ROLE_ADMIN: ROLE_USER
        ROLE_SUPER_ADMIN: [ROLE_ADMIN, ROLE_ALLOWED_TO_SWITCH]

    # http://symfony.com/doc/current/book/security.html#where-do-users-come-from-user-providers
    providers:
#        in_memory:
#            memory:
#                users:
#                    user: { password : userpass, roles:['ROLE_USER']}
#                    admin: { password : adminpass, roles:['ROLE_ADMIN']}
        secured_area:
            entity: {class: UserBundle\Entity\User, property: username}

    firewalls:
        # disables authentication for assets and the profiler, adapt it according to your needs
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

#        main:
#            anonymous: ~
#            # activate different ways to authenticate
#
#            # http_basic: ~
#            # http://symfony.com/doc/current/book/security.html#a-configuring-how-your-users-will-authenticate
#
#            # form_login: ~
#            # http://symfony.com/doc/current/cookbook/security/form_login_setup.html

        sercured_area:
            pattern: ^/
            anonymous: true
            provider: secured_area
            form_login:
                login_path: login
                check_path: login_check
            logout:
                path: logout
                target: /maison-editions
#            access_denied_url: /    #url de redirection pour utilisateur anonyme

    access_control:
        - { path: ^/maison-editions/list-articles, roles: ROLE_USER }
        - { path: ^/maison-editions/detail/update-article, roles: ROLE_ADMIN }